# Changelog

本项目遵循语义化版本。未完成目标真机验收的版本使用预发布标记。

## [Unreleased]

## [1.0.0] - 2026-08-25

### 正式版

- 将短信与忽略电池优化改为单项点按、系统确认、返回后自动复核；厂商启动管理使用可解析入口和安全回退，不再触发“没有应用可执行此操作”。
- 后台守护不再把一个手动确认伪装成三个可读取权限；网络改为实时状态，旅行守护会列出真实阻塞项并引导到首个待办。
- SMTP 加密方式与推荐端口双向一致，加入飞书邮箱预设，并按认证、DNS、TLS、超时、地址拒绝分类提示，不显示底层敏感异常。
- 更新检查加入稳定版 / Beta 通道；后续预发布与正式版共用正式签名，可覆盖安装并保留本机配置。
- 所有关键确认框统一为月白青玉玻璃卡片，并使用短时透明度与缩放动画。

### 安全与可靠性

- 配置导入在写入任何偏好前完成全量校验，避免畸形规则导致原配置被部分清空。
- 短信去重标识改为 Android Keystore HMAC-SHA-256，避免可离线枚举短信正文。
- 发布流水线同时支持稳定版与 Beta，并将 APK 签名证书与仓库信任锚进行比对。
- JVM 单元测试、Android Lint、Debug/Release 构建、API 35 权限与异常路径回归以及标准安全扫描通过。

## [0.3.0-beta.5] - 2026-08-25

### 页面路由与交互一致性

- 将设置页九个入口改为互不重复的目的地：邮箱、规则、后台授权、状态心跳、隐私、配置迁移、维护诊断、平台能力和开源许可分别进入对应页面。
- 新增独立的心跳管理、隐私安全、无密码配置迁移、平台能力边界和开源组件说明页面；维护页只保留诊断、队列清理和版本更新职责。
- 系统守护页不再重复承载心跳编辑器，改为清晰的心跳入口；页面返回关系继续保持“设置”一级导航选中。
- 重做原生下拉框：统一月白玻璃弹层、圆角、青玉选中态、勾选标识、展开箭头、48 dp 选项高度和辅助功能描述。

### 验证状态

- Android 15 / API 35 虚拟设备逐一点击九个设置入口，确认九个目标页面均不同；邮箱预设与加密方式下拉选择、返回关系和底部导航状态通过回归。
- JVM 单元测试、Android Lint、Debug/Release APK 构建通过；新增页面与下拉框视觉证据保存在 `docs/design/liquid-neumorphism-v2/audit-routing-dropdowns/`。

## [0.3.0-beta.4] - 2026-08-25

### 定稿视觉一致性

- 以八张“月白青玉”定稿为唯一视觉基线，重做页面安全区、标题留白、卡片密度、青玉配色、玻璃渐变、柔和阴影和底部导航比例。
- 统一桌面图标、方形品牌标和自由飞雁页首，补齐邮箱、规则、记录、系统守护、后台授权、维护诊断与设置总览的定稿结构。
- 邮箱主备通道改为玻璃页签与紧凑表单；记录页改为单卡时间线；系统守护清单增加逐行状态图标与完整授权层级。
- 修复首次生命周期重复构建页面和沉浸式窗口初始化时机造成的启动崩溃风险，保留系统减少动画时的即时降级。

### 验证状态

- Android 15 / API 35、1080 × 2400、420 dpi 隔离虚拟设备完成八页同画布对照、主备页签、键盘、130% 字号、减少动画和导航压力回归。
- JVM 单元测试、Android Lint、Debug/Release APK 构建通过；软件渲染虚拟机只用于布局与交互回归，最终动画手感和厂商后台策略仍需真机确认。

## [0.3.0-beta.3] - 2026-08-24

### 视觉与交互校正

- 修复 Android 27+ 主题覆盖丢失 `NoActionBar` 父主题、导致系统黑色标题栏遮挡品牌页首的问题，并适配 Android 15 强制边到边窗口的系统栏安全区。
- 按最终“月白青玉”基线重构八个页面：折纸飞雁品牌页首、软拟态内容分组、连续玻璃导航与选中胶囊、青玉渐变主操作和统一图标体系。
- 重做旅行守护状态面板、单行四项门槛、邮箱主备通道、规则折叠分组、记录筛选与空状态、系统授权进度、后台设置路径和诊断维护页面。
- 将记录筛选、主备 SMTP 页签和规则展开收起改为真实可交互状态，并补充辅助功能描述、130% 字号适配与系统关闭动画时的降级行为。
- 去除成本较高的全屏缩放和多层子元素同时入场动画，保留 140 ms 按压、220 ms 选中反馈和 300 ms 页面/展开动效。

### 验证状态

- 八个页面在 Android 15 / API 35、1080 × 2400、420 dpi 隔离虚拟设备中完成逐屏对照，验证截图随仓库保存。
- JVM 单元测试、Android Lint、Debug/Release APK 构建、减少动画模式和放大字号检查通过。
- 厂商电池管理、自启动白名单、真实 SIM/基带、真实 SMTP 和多日锁屏仍需真机验收。

## [0.3.0-beta.2] - 2026-08-24

### 可靠性与测试

- 修复转发工作线程用 `REPLACE` 替换正在运行的自身、造成连续取消日志的问题；内部续排改为追加后继任务，新短信触发仍可及时刷新调度。
- Debug 构建允许系统截图，便于自动化视觉回归；Release 构建继续启用安全窗口，保护短信与邮箱配置。
- 在隔离的 Android 15 / API 35 虚拟设备中验证普通短信、验证码过滤、加密重试队列、锁屏进程回收和系统重启恢复。
- 修正设置页的本机加密说明，使其准确反映授权码与短信敏感数据的加密范围。

### 验证状态

- JVM 单元测试、Android Lint、Debug/Release APK 构建及 GitHub Actions 通过。
- 厂商电池管理、自启动白名单、真实 SIM/基带、真实 SMTP 和多日锁屏仍需真机验收。

## [0.3.0-beta.1] - 2026-08-24

### 界面与交互

- 按“月白青玉”设计稿重构原生界面：统一桌面图标与页面 Logo，采用轻量软拟态内容层和液态玻璃一级导航。
- 将一级导航收敛为“记录 / 规则 / 守护 / 设置”，新增设置总览、系统守护、维护诊断与后台授权引导页面。
- 增加统一 Motion 规范：页面切换、导航选中、按钮按压与展开收起使用硬件加速短动画，并在系统关闭动画时自动降级。

### 工程与发布

- 修复主内容滚动区宽度被设为 0，导致真机只能看到品牌栏和导航、所有页面内容不可见的问题。
- 增加每天一次的官方 GitHub Release 自动检查和手动检查，不静默下载或安装。
- 增加环境变量正式签名、APK 验证、SHA-256、CycloneDX SBOM 和 GitHub 构建来源证明流水线。
- 将公开验证材料改为设备无关表述，不记录个人设备具体型号或系统补丁版本。

### 验证状态

- JVM 单元测试、Android Lint、Debug APK 和 Release APK 构建通过。
- 原生视觉对比、动画帧率、锁屏与多日稳定性仍需真实设备验收。

## [0.2.0-beta.1] - 2026-08-24

### 新增

- “雁笺”中国风品牌、图标与现代五页式交互界面。
- 主/备用 SMTP、多收件人、失败切换和双通道投递。
- 发件人、正文、验证码、SIM、星期、时段与正文脱敏规则。
- 旅行守护、真实短信闭环门槛、定时心跳、低电量/断电提醒和华为后台设置引导。
- 加密历史、失败重发、配置迁移、脱敏诊断报告与规则预览。

### 可靠性与安全

- 使用 WorkManager 调度联网发送和断网恢复，增加消息并发租约、稳定 ID、分通道投递状态与指数退避。
- SMTP 授权码、待发短信和历史记录使用 Android Keystore 支持的 AES-GCM 加密。
- 强制 TLS/STARTTLS 和证书主机名校验；关闭应用备份、界面截图和最近任务缩略图。
- 暂停或配置错误时停止无效后台重排，同时保留待发队列。
- 用户正则改用线性时间引擎；限制单条短信和待发队列容量，并合并重复后台调度，避免资源耗尽。

### 验证状态

- 已通过 JVM 单元测试、Android Lint、Debug APK 和 Release APK 构建。
- 锁屏、断网恢复与多日稳定性尚待受支持的真实设备验收。

## [0.1.0-alpha] - 2026-08-23

- 建立纯客户端 SMTP 直连原型、平台支持边界、基础加密队列和 GitHub Actions。
