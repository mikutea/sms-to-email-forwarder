# HarmonyOS NEXT 客户端状态

本目录暂不包含可发布 HAP。原因不是 SMTP：NEXT 应用可以进行网络通信；阻塞点是普通三方应用没有读取系统新短信正文的公开能力。

只有在以下任一前置条件成立后才会创建原生客户端：

1. 华为公开并允许本项目场景申请接收新短信正文的能力；或
2. 短信入口迁移到运营商网关、独立蜂窝模块或受控 Android/鸿蒙 4.x 设备。

在条件未满足前，不使用私有 API、通知内容抓取或无障碍模拟点击，也不发布一个声称能够自动转发短信但实际上做不到的 NEXT 安装包。

