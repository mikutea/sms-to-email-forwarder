# 隐私与安全设计

## 数据流与最小权限

雁笺只申请网络、接收新短信、开机恢复、网络状态、请求忽略电池优化和发起自身更新安装所需权限。它不申请历史短信、通讯录、通话记录、存储、无障碍或 Root 权限。安装来源授权只在用户主动下载更新且系统要求时引导开启；它不能绕过系统安装确认，也不能静默安装其他应用。

“转发成功后标记系统短信已读”是独立且默认关闭的可选能力。用户开启时，Android 会授予设备级通知使用权，其系统范围可能包含其他应用通知。雁笺只在真实短信已被 SMTP 接受后，短时检查默认短信应用通知，并只请求语义明确的标准“标记已读”动作；最终已读状态由默认短信应用处理，雁笺不会仅凭动作已分发就宣称结果已经完成。它不把其他通知正文写入队列、历史或诊断报告。授权撤销、动作缺失、匹配不唯一或等待超时都不会影响邮件已发送状态。

新短信由系统广播交给 App 后先写入本机加密队列，发送任务再通过用户配置的 SMTP 服务器投递。没有项目运营的中继服务器，也没有遥测上传。

## 本机数据

- SMTP 授权码、待发短信发件人/正文和历史记录使用 Android Keystore 支持的 AES-GCM 加密。
- 应用备份和设备迁移提取均关闭。
- 敏感界面启用 `FLAG_SECURE`，阻止普通系统截图和最近任务缩略图。
- 历史通常保留最近 600 条和 90 天；尚在待发队列或临时已读联动中的记录不会因数量裁剪提前消失。用户可单独清空历史或待发队列。
- 可选已读联动只保留待匹配发件方和原短信最多 16 个规范化字符的匹配线索，并使用 AES-GCM 加密；它不会为匹配额外保存完整原文。成功后的临时请求最多 50 条，有效期严格从 SMTP 成功起计算 10 分钟。过期项在任何读取前即被排除，持久 WorkManager 清理任务和数据库重开会删除其密文；系统极端省电可能推迟物理删除，但不会延长其可用或可操作时间。

配置导出包含 SMTP 主机、用户名、邮箱地址和规则，不包含授权码、短信正文或历史。导入后自动关闭转发和旅行守护，并要求重新填写授权码、隐私确认与后台确认。

## 网络与投递

- 仅支持建连即 TLS 或强制 STARTTLS。
- 始终启用服务器证书主机名校验，不提供跳过校验选项。
- SMTP 调试日志关闭；状态与诊断报告不包含授权码和短信正文。
- HTML 邮件对发送方和正文执行上下文转义，主题移除回车换行并限制长度；模板不加载第三方远程资源。
- SMTP 成功响应只能证明服务器接受了邮件，不能证明最终进入收件箱。

启用自动更新检查后，App 在用户已经确认短信外发隐私风险的前提下，启动时每天最多访问一次本项目公开 GitHub Releases API。请求不包含短信、邮箱配置或设备标识。用户可选择稳定版或 Beta 通道并关闭自动检查。主动下载时只接受本仓库对应版本的官方 APK 资产，限制 HTTPS 跳转主机和文件大小，并在调用系统安装器前校验 GitHub 资产 SHA-256、包名、版本号、版本代码以及与当前安装版本完全一致的签名证书。APK 暂存在应用私有缓存，不申请共享存储权限；失败、取消或缓存清理不会改变当前版本和配置。

## 威胁边界

短信发件人、其他本地 App、导入文件和 SMTP 服务端均按不可信输入处理。系统短信接收器受 `BROADCAST_SMS` 权限保护；其他后台组件不对外导出。用户正则使用 RE2/J 线性时间引擎；单条短信正文、待发队列行数/密文体积和导入文件均设有上限，后台发送任务按唯一名称合并。

设备已 Root、系统被攻破、攻击者已解锁并控制设备，或恶意键盘读取输入等情形不在普通 App 可防御范围内。真正需要企业级远程可观测和严格一次投递时，应使用受控短信网关，而不是纯手机直连 SMTP。
