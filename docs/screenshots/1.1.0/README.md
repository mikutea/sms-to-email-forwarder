# v1.1.0 application screenshots

These screenshots were captured from the current `v1.1.0` debug build on an isolated Android 15 / API 35 virtual device. All messages, email addresses, timestamps and device states are synthetic test data.
